import { DrawerPageProps } from "config/@types/PersistentDrawer";
import React from "react";

const DrawerPage = React.forwardRef((props: DrawerPageProps, ref) => {
  return null;
});

export default DrawerPage;
